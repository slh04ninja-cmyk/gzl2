name: RDP + Tailscale + RustDesk + TradingBot (A)

on:
  workflow_dispatch:
    inputs:
      ts_tailnet:         { description: "Tailscale tailnet (e.g. you@gmail.com)", required: true }
      ts_api_key:         { description: "Tailscale API key", required: true }
      ts_authkey:         { description: "Tailscale Auth key", required: true }
      quick_test:         { description: "Run 5-minute test", type: boolean, default: false }
      runtime_minutes:    { description: "Runtime (max 360)", required: false, default: "355" }
      do_purge:           { description: "Purge bullet* devices at start", required: false, default: "true" }
      cycles:             { description: "0=stop after A; N=handoffs left", required: false, default: "0" }
      rdp_count:          { description: "RDP instances (1-10)", required: false, default: "1" }
      crd_code:           { description: "Chrome Remote Desktop code (optional)", required: false, default: "" }
      crd_pin:            { description: "CRD PIN (6-12 digits)", required: false, default: "123456" }
      bot_repo:
        description: "Votre repo GitHub (ex: slh02ninja-cmyk/gzl1)"
        required: true
        default: ""
      tg_api_id:
        description: "Telegram API ID (my.telegram.org)"
        required: true
        default: ""
      tg_api_hash:
        description: "Telegram API Hash"
        required: true
        default: ""
      tg_channel:
        description: "Canal Telegram 1 (ex: @MonCanal)"
        required: true
        default: ""
      tg_channel_2:
        description: "Canal Telegram 2 (optionnel)"
        required: false
        default: ""
      tg_channel_3:
        description: "Canal Telegram 3 (optionnel)"
        required: false
        default: ""
      tg_channel_4:
        description: "Canal Telegram 4 (optionnel)"
        required: false
        default: ""
      tg_report_channel:
        description: "Canal de rapport Telegram (alertes + récap)"
        required: false
        default: ""
      mt5_login:
        description: "MT5 Login (numero de compte)"
        required: true
        default: ""
      mt5_password:
        description: "MT5 Password"
        required: true
        default: ""
      mt5_server:
        description: "MT5 Server (ex: Exness-MT5Real16)"
        required: true
        default: ""
      lot_total:
        description: "Lot total par signal (ex: 0.02)"
        required: false
        default: "0.02"
      trail_points:
        description: "Trail points pour XAUUSDm Exness (150 = $1.50)"
        required: false
        default: "150"
      demo_mode:
        description: "Demo Mode — true=analyse (conflit désactivé), false=live"
        required: false
        default: "true"
      news_filter_enabled:
        description: "Filtre News Forex Factory (true/false)"
        required: false
        default: "true"
      news_window_before_block:
        description: "Minutes avant news → bloquer signaux"
        required: false
        default: "15"
      news_window_before_close:
        description: "Minutes avant news → fermer positions"
        required: false
        default: "5"
      news_window_after:
        description: "Minutes après news → reprendre"
        required: false
        default: "15"
      time_filter_enabled:
        description: "Filtre Horaire (true/false)"
        required: false
        default: "true"
      time_blocked_windows:
        description: "Fenêtres bloquées UTC (ex: 13:00-15:00,16:3
        default:0-17:30)"
        required: false "13:00-15:00,16:30-17:30"

permissions:
  contents: read
  actions: write

defaults:
  run:
    shell: pwsh

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.mk.outputs.matrix }}
      multi:  ${{ steps.mk.outputs.multi }}
    steps:
      - id: mk
        run: |
          $crd = @'
          ${{ inputs.crd_code }}
          '@.Trim()
          if ($crd.Length -gt 0) { $n = 1 } else {
            $n = [int]"${{ inputs.rdp_count }}"
            if ($n -lt 1) { $n = 1 }
            if ($n -gt 10) { $n = 10 }
          }
          $inc = @()
          for ($i=1; $i -le $n; $i++){ $inc += @{ id = $i } }
          $json = @{ include = $inc } | ConvertTo-Json -Compress
          "matrix=$json" | Out-File $env:GITHUB_OUTPUT -Append -Encoding utf8
          $multi = if ($n -gt 1) { '1' } else { '0' }
          "multi=$multi" | Out-File $env:GITHUB_OUTPUT -Append -Encoding utf8

  rdp:
    needs: setup
    runs-on: windows-latest
    strategy:
      matrix: ${{ fromJson(needs.setup.outputs.matrix) }}
      max-parallel: 10
    timeout-minutes: 370
    env:
      RDP_USER: Bullettemporary
      RDP_PASS: Bullet@12345

    steps:
      - name: Set CRD flag
        run: |
          $crd = @'
          ${{ inputs.crd_code }}
          '@.Trim()
          if ($crd.Length -gt 0) {
            "CRD_ENABLED=1" | Out-File -Append $env:GITHUB_ENV
          } else {
            "CRD_ENABLED=0" | Out-File -Append $env:GITHUB_ENV
          }

      - name: Install Chrome Remote Desktop (if needed)
        if: env.CRD_ENABLED == '1'
        run: |
          $exe = "${Env:PROGRAMFILES(X86)}\Google\Chrome Remote Desktop\CurrentVersion\remoting_start_host.exe"
          if (-not (Test-Path $exe)) {
            $exePath = "$env:TEMP\chromeremotedesktophost.exe"
            try {
              Invoke-WebRequest -Uri "https://dl.google.com/support/remote_assistance/ChromotingHostSetup.exe" -OutFile $exePath -ErrorAction Stop
              Start-Process -FilePath $exePath -ArgumentList "/silent","/install" -Wait
              Remove-Item $exePath -Force
            } catch {
              $msi = "$env:TEMP\chromeremotedesktophost.msi"
              Invoke-WebRequest -Uri "https://dl.google.com/edgedl/chrome-remote-desktop/chromeremotedesktophost.msi" -OutFile $msi -ErrorAction Stop
              Start-Process msiexec.exe -ArgumentList "/i","`"$msi`"","/quiet","/norestartForce
            }
" -Wait
          }

      - name: Run Chrome Remote Desktop
        if: env.CRD_ENABLED == '1'
        run: |
          $raw = @'
          ${{ inputs.crd_code }}
          '@.Trim()
          $pin     = "${{ inputs.crd_pin }}".Trim()
          $starter = "${Env:PROGRAMFILES(X86)}\Google\Chrome Remote Desktop\CurrentVersion\remoting_start_host.exe              Remove-Item $msi -"
          if ($raw -match '--code\s*=\s*"?([^"\s]+)') {
            $code = ([regex]::Match($raw,'--code\s*=\s*"?([^"\s]+)')).Groups[1].Value
          } elseif ($raw -match '^4\/') {
            $code = $raw
          }
          if ($code) {
            Start-Process -FilePath $starter -ArgumentList @(
              "--code=""$code""",
              "--redirect-url=""https://remotedesktop.google.com/_/oauthredirect""",
              "--name=""$Env:COMPUTERNAME""",
              "--pin=""$pin"""
            )
          }

      - name: Decide hostname + runtime
        run: |
          if ("${{ inputs.rdp_count }}" -gt 1) { $hn = "bullet${{ matrix.id }}" } else { $hn = "bullet" }
          "TS_HOSTNAME=$hn" | Out-File -Append $env:GITHUB_ENV
          function Yes($v){ "$v" -match '^(?i:true|1|yes|on)$' }
          if ("${{ inputs.runtime_minutes }}" -match '^\d+$') {
            $runtime = [int]"${{ inputs.runtime_minutes }}"
          } else {
            $runtime = 355
          }
          if (Yes("${{ inputs.quick_test }}")) { $runtime = 5 }
          if (-not (Yes("${{ inputs.quick_test }}")) -and $runtime -lt 6) { $runtime = 355 }
          if ($runtime -gt 360) { $runtime = 355 }
          "RUNTIME_MINUTES=$runtime" | Out-File -Append $env:GITHUB_ENV

      - name: Purge bullet* devices
        if: ${{ needs.setup.outputs.multi == '0' }}
        run: |
          function Yes($v){ "$v" -match '^(?i:true|1|yes|on)$' }
          if (Yes("${{ inputs.do_purge }}")) {
            try {
              $auth = [Convert]::ToBase64String([Text.Encoding]::ASCII.GetBytes("${{ inputs.ts_api_key }}:"))
              $tn   = [uri]::EscapeDataString("${{ inputs.ts_tailnet }}")
              $list = Invoke-RestMethod -Uri "https://api.tailscale.com/api/v2/tailnet/$tn/devices" -Headers @{ Authorization = "Basic $auth" }
              foreach ($d in $list.devices) {
                if ($d.hostname -match '^bullet[0-9]*$') {
                  Invoke-RestMethod -Method Delete -Uri "https://api.tailscale.com/api/v2/device/$($d.id)" -Headers @{ Authorization = "Basic $auth" } -EA SilentlyContinue
                }
              }
              Write-Host "Purge terminee."
            } catch { Write-Host "Purge warning: $($_.Exception.Message)" }
          }

      - name: Install + Up Tailscale
        run: |
          $ts = "$env:ProgramFiles\Tailscale\t $ts up --authkey "${{ inputs.ts_authkey }}" --hostname "$env:TS_HOSTNAME" --accept-dns=true --accept-routes=true
          $ip4 = & $ts ip -4 | Select-Object -First 1
          "TAILSCALE_IP=$ip4" | Out-File -Append $env:GITHUB_ENV
          Write-Host "Tailscale IPv4: $ip4"

      - name: Enable RDP user + firewall
        run: |
          $u   = $env:RDP_USER
          $p   = $env:RDP_PASS
          $sec = ConvertTo-SecureString $p -AsPlainText -Force
          if (-not (Get-LocalUser -Name $u -EA SilentlyContinue)) {
            New-LocalUser -Name $u -Password $sec -AccountNeverExpires
            Add-LocalGroupMember -Group "Administrators" -Member $u
            Add-LocalGroupMember -Group "Remote Desktop Users" -Member $u
          } else {
            Set-LocalUser -Name $u -Password $sec -AccountNeverExpires
            Enable-LocalUser -Name $u
            Add-LocalGroupMember -Group "Administrators" -Member $u -EA SilentlyContinue
            Add-LocalGroupMember -Group "Remote Desktop Users" -Member $u -EA SilentlyContinue
          }
          Set-ItemProperty "HKLM:\System\CurrentControlSet\Control\Terminal Server" -Name fDenyTSConnections -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop" | Out-Null
          Write-Host "RDP pret. user=$u | ip=$env:TAILSCALE_IP"

      - name: Install Python 3.11 + add to PATH
        run: |
          $pyCmd = Get-Command python -ErrorAction SilentlyContinue
          if ($pyCmd) {
            $ver = & python --version 2>&1
            Write-Host "Python déjà présent : $ver"
          } else {
            Write-Host "Python non trouvé — installation en cours..."
            $pyUrl = "https://www.python.org/ftp/python/3.11.9/python-3.11.9-amd64.exe"
            $pyInst = "$env:TEMP\python-installer.exe"
            Invoke-WebRequest -Uri $pyUrl -OutFile $pyInst
            Start-Process -FilePath $pyInst -ArgumentList @(
              "/quiet",
              "InstallAllUsers=1",
              "PrependPathstart" -Wait
            Remove-ailscale.exe"
          if (-not (Test-Path $ts)) {
            $dst = "$env:TEMP\tailscale.msi"
            Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi" -OutFile $dst
            Start-Process msiexec.exe -ArgumentList "/i","`"$dst`"","/quiet","/noreItem $dst -Force
          }
          & $ts logout | Out-Null
          &=1",
              "Include_test=0",
              "Include_doc=0"
            ) -Wait
            Remove-Item $pyInst -Force
            Write-Host "Python installé."
          }
          $machinePath = [System.Environment]::GetEnvironmentVariable("Path", "Machine")
          $userPath    = [System.Environment]::GetEnvironmentVariable("Path", "User")
          $env:Path    = "$machinePath;$userPath"
          $pyPaths = @(
            "C:\Python311",
            "C:\Python311\Scripts",
            "C:\Program Files\Python311",
            "C:\Program Files\Python311\Scripts",
            "$env:LOCALAPPDATA\Programs\Python\Python311",
            "$env:LOCALAPPDATA\Programs\Python\Python311\Scripts"
          )
          foreach ($p in $pyPaths) {
            if (Test-Path "$p\python.exe") {
              Write-Host "Python trouvé dans : $p"
              $current = [System.Environment]::GetEnvironmentVariable("Path", "Machine")
              if ($current -notlike "*$p*") {
                [System.Environment]::SetEnvironmentVariable("Path", "$current;$p;$p\Scripts", "Machine")
                Write-Host "PATH mis à jour avec $p"
              }
              $env:Path = "$env:Path;$p;$p\Scripts"
              break
            }
          }
          $ver = & python --version 2>&1
          Write-Host "Version finale : $ver"
          "PYTHON_OK=1" | Out-File -Append $env:GITHUB_ENV
      - name: Install Python libs (avec gspread)
        run: |
          $machinePath = [System.Environment]::GetEnvironmentVariable("Path", "Machine")
          $userPath    = [System.Environment]::GetEnvironmentVariable("Path", "User")
          $env:Path    = "$machinePath;$userPath"

          python -m pip install --upgrade pip -q
          python -m pip install telethon MetaTrader5 python-dotenv gspread google-auth -q
          python -c "import telethon, MetaTrader5, dotenv, gspread, google.auth; print('Python libs OK')"

      - name: Install RustDesk
        run: |
          $rdExe = "C:\Program Files\RustDesk\rustdesk.exe"
          if (-not (Test-Path $rdExe)) {
            try {
              $dst = "$env:TEMP\rustdesk.msi"
              Invoke-WebRequest -Uri "https://github.com/rustdesk/rustdesk/releases/download/1.3.1/rustdesk-1.3.1-x86_64.msi" -OutFile $dst
              Start-Process msiexec.exe -ArgumentList "/i","`"$dst`"","/quiet","/norestart" -Wait
              Remove-Item $dst -Force
            } catch {
              winget install --id RustDesk.RustDesk -e --silent --accept-package-agreements --accept-source-agreements --disable-interactivity
            }
          }
          Start-Process -FilePath $rdExe -EA SilentlyContinue
          Start-Sleep -Seconds 5

      - name: Download MT5 Exness
        run: |
          try {
            $desktop = [Environment]::GetFolderPath("CommonDesktopDirectory")
            $mt5Dest = "$desktop\exness5setup.exe"
            $mt5Url  = "https://download.terminal.free/cdn/web/exness.technologies.ltd/mt5/exness5setup.exe"
            Write-Host "Telechargement MT5 Exness..."
            Invoke-WebRequest -Uri $mt5Url -OutFile $mt5Dest -TimeoutSec 60 -ErrorAction Stop
            Write-Host "MT5 telecharge sur le Bureau : $mt5Dest"
          } catch {
            Write-Host "MT5 download ignore : $($_.Exception.Message)"
          }

      - name: Deploy HTF_Gold_EA_v3.2 to Desktop
        env:
          GH_TOKEN: ${{ github.token }}
          BOT_REPO: ${{ inputs.bot_repo }}
        run: |
          try {
            $desktop = [Environment]::GetFolderPath("CommonDesktopDirectory")
            $base    = "https://raw.githubusercontent.com/$env:BOT_REPO/main"
            $headers = @{ Authorization = "Bearer $env:GH_TOKEN" }
            $eaDest  = "$desktop\HTF_Gold_EA_v3.2.mq5"
            Invoke-WebRequest -Uri "$base/HTF_Gold_EA_v3.2.mq5" -OutFile $eaDest -Headers $headers -ErrorAction Stop
            Write-Host "HTF_Gold_EA_v3.2.mq5 copie sur le Bureau : $eaDest"
          } catch {
            Write-Warning "Impossible de telecharger HTF_Gold_EA_v3.2.mq5 : $($_.Exception.Message)"
          }

      - name: Copy MQ5 files to Desktop
        env:
          GH_TOKEN: ${{ github.token }}
          BOT_REPO: ${{ inputs.bot_repo }}
        run: |
          $desktop = [Environment]::GetFolderPath("CommonDesktopDirectory")
          $base    = "https://raw.githubusercontent.com/$env:BOT_REPO/main"
          $headers = @{ Authorization = "Bearer $env:GH_TOKEN" }
          $files   = @(
            "HTF_Gold_EA_v4.0.mq5",
            "HTF_Gold_EA_v4.1.mq5",
            "HTF_Gold_EA_v4.2.mq5",
            "HTF_Gold_EA_v4.3.mq5",
            "HTF_Gold_EA_v4.3.1.mq5",
            "HTF_Gold_EA_v3.4.mq5",
            "HTF_Gold_EA_v3.4.final.set"
          )
          foreach ($f in $files) {
            try {
              Invoke-WebRequest -Uri "$base/$f" -OutFile "$desktop\$f" -Headers $headers -ErrorAction Stop
              Write-Host "$f copie sur le Bureau"
            } catch {
              Write-Host "$f ignore : $($_.Exception.Message)"
            }
          }
      - name: Setup TradingBot (4 canaux + Google Sheets + Demo Mode)
        env:
          GH_TOKEN:                  ${{ github.token }}
          BOT_REPO:                  ${{ inputs.bot_repo }}
          TG_API_ID:                 ${{ inputs.tg_api_id }}
          TG_API_HASH:               ${{ inputs.tg_api_hash }}
          TG_CHANNEL:                ${{ inputs.tg_channel }}
          TG_CHANNEL_2:              ${{ inputs.tg_channel_2 }}
          TG_CHANNEL_3:              ${{ inputs.tg_channel_3 }}
          TG_CHANNEL_4:              ${{ inputs.tg_channel_4 }}
          TG_REPORT_CHANNEL:         ${{ inputs.tg_report_channel }}
          MT5_LOGIN:                 ${{ inputs.mt5_login }}
          MT5_PASSWORD:              ${{ inputs.mt5_password }}
          MT5_SERVER:                ${{ inputs.mt5_server }}
          LOT_TOTAL:                 ${{ inputs.lot_total }}
          TRAIL_POINTS:              ${{ inputs.trail_points }}
          DEMO_MODE:                 ${{ inputs.demo_mode }}
          NEWS_FILTER_ENABLED:       ${{ inputs.news_filter_enabled }}
          NEWS_WINDOW_BEFORE_BLOCK:  ${{ inputs.news_window_before_block }}
          NEWS_WINDOW_BEFORE_CLOSE:  ${{ inputs.news_window_before_close }}
          NEWS_WINDOW_AFTER:         ${{ inputs.news_window_after }}
          TIME_FILTER_ENABLED:       ${{ inputs.time_filter_enabled }}
          TIME_BLOCKED_WINDOWS:      ${{ inputs.time_blocked_windows }}
        run: |
          # ── Créer le dossier ──
          $botDir = "C:\TradingBot"
          if (-not (Test-Path $botDir)) { New-Item -ItemType Directory -Path $botDir | Out-Null }

          # ── Télécharger le bot depuis GitHub ──
          $base    = "https://raw.githubusercontent.com/$env:BOT_REPO/main"
          $headers = @{ Authorization = "Bearer $env:GH_TOKEN" }
          try {
            Invoke-WebRequest -Uri "$base/telegram_listener_v2.py" -OutFile "$botDir\telegram_listener_v2.py" -Headers $headers -ErrorAction Stop
            Write-Host "telegram_listener_v2.py telecharge"
          } catch {
            Write-Warning "Impossible de telecharger telegram_listener_v2.py : $($_.Exception.Message)"
          }

          # ── Télécharger google_credentials.json G_API_HASH",
            "TG_CHANNEL=$env:TG_CHANNEL",
            "TG_CHANNEL_2=$env:TG_CHANNEL_2",
            "TG_CHANNEL_3=$env:TG_CHANNEL_3",
            "TG_CHANNEL_4=$env:TG_CHANNEL_4",
            "TG_REPORT_CHANNEL=$env:TG_REPORT_CHANNEL",
            "MT5_LOGIN=$env:MT5_LOGIN",
            "MT5_PASSWORD=$env:MT5_PASSWORD",
            "MT5_SERVER=$env:MT5_SERVER",
            "LOT_TOTAL=$env:LOT_TOTAL",
            "ORDER_EXPIRY_MINUTES=240",
            "SLIPPAGE=20",
            "MAGIC_NUMBER=20250226",
            "TRAIL_POINTS=$env:TRAIL_POINTS",
            "DEMO_MODE=$env:DEMO_MODE",
            "NEWS_FILTER_ENABLED=$env:NEWS_FILTER_ENABLED",
            "NEWS_WINDOW_BEFORE_BLOCK=$env:NEWS_WINDOW_BEFORE_BLOCK",
            "NEWS_WINDOW_BEFORE_CLOSE=$env:NEWS_WINDOW_BEFORE_CLOSE",
            "NEWS_WINDOW_AFTER=$env:NEWS_WINDOW_AFTER",
            "TIME_FILTER_ENABLED=$env:TIME_FILTER_ENABLED",
            "TIME_BLOCKED_WINDOWS=$env:TIME_BLOCKED_WINDOWS"
          )
          $lines | Set-Content -Path $envFile -Encoding UTF8
          Write-Host "Fichier .env cree"

          # ── Raccourci Bureau ──
          $desktop = [Environment]::GetFolderPath("CommonDesktopDirectory")
          $wsh = New-Object -ComObject WScript.Shell
          $lnk = $wsh.CreateShortcut("$desktop\Lancer Bot Trading.lnk")
          $──
          try {
            Invoke-WebRequest -Uri "$base/google_credentials.json" -OutFile "$botDir\google_credentials.json" -Headers $headers -ErrorAction Stop
            Write-Host "google_credentials.json telecharge"
          } catch {
            Write-Warning "google_credentials.json non trouve dans le repo (optionnel)"
          }

          # ── Créer le fichier .env ──
          $envFile = "$botDir\.env"
          $lines = @(
            "TG_API_ID=$env:TG_API_ID",
            "TG_API_HASH=$env:Tlnk.TargetPath       = "powershell.exe"
          $lnk.Arguments        = "-NoExit -Command `"cd C:\TradingBot; python telegram_listener_v2.py`""
          $lnk.WorkingDirectory = $botDir
          $lnk.IconLocation     = "powershell.exe"
          $lnk.Save()

          # ── Résumé ──
          Write-Host "========================================"
          Write-Host "  BOT TRADING PRET"
          Write-Host "========================================"
          Write-Host "  IP RDP     : $env:TAILSCALE_IP"
          Write-Host "  User RDP   : Bullettemporary"
          Write-Host "  Pass RDP   : Bullet@12345"
          Write-Host "  Canal 1    : $env:TG_CHANNEL"
          if ($env:TG_CHANNEL_2) { Write-Host "  Canal 2    : $env:TG_CHANNEL_2" }
          if ($env:TG_CHANNEL_3) { Write-Host "  Canal 3    : $env:TG_CHANNEL_3" }
          if ($env:TG_CHANNEL_4) { Write-Host "  Canal 4    : $env:TG_CHANNEL_4" }
          if ($env:TG_REPORT_CHANNEL) { Write-Host "  Rapport    : $env:TG_REPORT_CHANNEL" }
          Write-Host "  MT5        : $env:MT5_SERVER"
          Write-Host "  Lots       : $env:LOT_TOTAL"
          Write-Host "  Trail      : $env:TRAIL_POINTS pts"
          Write-Host "  Demo Mode  : $env:DEMO_MODE"
          Write-Host "========================================"
          Write-Host "  Sur le Bureau : double-cliquez"
          Write-Host "  'Lancer Bot Trading'"
          Write-Host "========================================"
      - name: Keep alive
        run: |
          $mins = [int]"$env:RUNTIME_MINUTES"
          $end  = (Get-Date).AddMinutes($mins)
          while ((Get-Date) -lt $end) {
            Write-Host ("Heartbeat " + (Get-Date).ToString("HH:mm:ss") + " | ends " + $end.ToString("HH:mm:ss") + " | ip=$env:TAILSCALE_IP")
            Start-Sleep -Seconds 60
          }

      - name: Dispatch next or stop
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $cycles = [int]"${{ inputs.cycles }}"
          if ($cycles -gt 0) {
            $next = $cycles - 1
            if ($next -le 0) {
              $url  = "https://api.github.com/repos/${{ github.repository }}/actions/workflows/rdp-tailscale-stop.yml/dispatches"
              $hdr  = @{ Authorization = "Bearer $env:GH_TOKEN"; Accept = "application/vnd.github+json" }
              $body = @{
                ref    = "${{ github.ref_name }}"
                inputs = @{
                  ts_tailnet  = "${{ inputs.ts_tailnet }}"
                  ts_api_key  = "${{ inputs.ts_api_key }}"
                  base_prefix = "bullet"
                  max_count   = "10"
                  dry_run     = "false"
                }
              } | ConvertTo-Json -Depth 20
              Invoke-WebRequest -Method POST -Uri $url -Headers $hdr -Body $body | Out-Null
              Write-Host "Dispatched STOP."
            } else {
              $payload = @{
                ts_tailnet         = "${{ inputs.ts_tailnet }}"
                ts_api_key         = "${{ inputs.ts_api_key }}"
                ts_authkey         = "${{ inputs.ts_authkey }}"
                quick_test         = "${{ inputs.quick_test }}"
                runtime_minutes    = "${{ inputs.runtime_minutes }}"
                do_purge           = "${{ inputs.do_purge }}"
                cycles             = "$next"
                rdp_count          = "${{ inputs.rdp_count }}"
                crd_code           = "${{ inputs.crd_code }}"
                crd_pin            = "${{ inputs.crd_pin }}"
                bot_repo           = "${{ inputs.bot_repo }}"
                tg_api_id          = "${{ inputs.tg_api_id }}"
                tg_api_hash        = "${{ inputs.tg_api_hash }}"
                tg_channel         = "${{ inputs.tg_channel }}"
                tg_channel_2       = "${{ inputs.tg_channel_2 }}"
                tg_channel_3       = "${{ inputs.tg_channel_3 }}"
                tg_channel_4       = "${{ inputs.tg_channel_4 }}"
                tg_report_channel  = "${{ inputs.tg_report_channel }}"
                mt5_login          = "${{ inputs.mt5_login }}"
                mt5_password       = "${{ inputs.mt5_password }}"
                mt5_server         = "${{ inputs.mt5_server }}"
                lot_total          = "${{ inputs.lot_total }}"
                trail_points       = "${{ inputs.trail_points }}"
                demo_mode          = "${{ inputs.demo_mode }}"
                news_filter_enabled      = "${{ inputs.news_filter_enabled }}"
                news_window_before_block = "${{ inputs.news_window_before_block }}"
                news_window_before_close = "${{ inputs.news_window_before_close }}"
                news_window_after        = "${{ inputs.news_window_after }}"
                time_filter_enabled      = "${{ inputs.time_filter_enabled }}"
                time_blocked_windows     = "${{ inputs.time_blocked_windows }}"
              }
              $url2  = "https://api.github.com/repos/${{ github.repository }}/actions/workflows/rdp-tailscale-rustdesk-B.yml/dispatches"
              $hdr2  = @{ Authorization = "Bearer $env:GH_TOKEN"; Accept = "application/vnd.github+json" }
              $body2 = @{ ref = "${{ github.ref_name }}"; inputs = $payload } | ConvertTo-Json -Depth 20
              Invoke-WebRequest -Method POST -Uri $url2 -Headers $hdr2 -Body $body2 | Out-Null
              Write-Host "Dispatched Workflow B cycles=$next"
            }
          } else {
            Write-Host "Pas de cycle configure. Termine."
          }
          
